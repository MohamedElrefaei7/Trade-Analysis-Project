apiVersion: 1

datasources:
  - name: TimescaleDB
    uid: timescaledb
    type: postgres
    access: proxy
    url: timescaledb:5432
    user: admin
    jsonData:
      database: mydb
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
    secureJsonData:
      password: password
    isDefault: true
    editable: true
